# Memory Beast — Backend

FastAPI + Celery + PostgreSQL backend engine (MemoryWeb core).

Backend source code will be populated in the git publication phase.
See the root `docker-compose.yml` and `.env.example` for configuration.
